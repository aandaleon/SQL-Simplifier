1) IN TERMINAL, WILL HAVE TO TYPE:
easy_install pip  
pip install BeautifulSoup4

2) HTML TAGS REVIEW
Basic HTML Webpage syntax:
    <!DOCTYPE html>  
    <html>  
        <head>
        </head>
        <body>
            <h1> First Scraping </h1>
            <p> Hello World </p>
        <body>
    </html>
    a. <!DOCTYPE html>: HTML docs start with a type declaration.
    b. The HTML document is contained between <html> and </html>.
    c. The meta and script declaration of the HTML document is between <head> and </head>.
    d. The visible part of the HTML document is between <body> and </body> tags.
    e. Title headings are defined with the <h1> through <h6> tags.
    f. Paragraphs are defined with the <p> tag.
    g. Other useful tags include <a> for hyperlinks, <table> for tables, <tr> for table rows, and <td> for table columns.
    
3) IMPORTING LIBRARIES
import urllib2
from bs4 import BeautifulSoup

4) SPECIFY THE URL
quote_page = 'http://amigo.geneontology.org/amigo/term/GO:0071828'

5) QUERY THE WEBSITE AND RETURN HTML TO THE VARIABLE PAGE
page = urllib2.urlopen(quote_page)

6) PARSE HTML USING BEAUTIFUL SOUP AND STORE IN THE VARIABLE SOUP
soup = BeautifulSoup(page, ‘html.parser’)

7) USING FIND EXTRACT INFO FROM WEBPAGE
# Take out the <div> of name and get its value
name_box = soup.find(‘h1’, attrs={‘class’: ‘name’}

8) GETTING TEXT DATA
#Now that we have specified a tag we can get the text
name = name_box.text.strip() 
print name
